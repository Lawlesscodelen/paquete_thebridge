from setuptools import setup

setup(
    name='funciones',
    author = 'elen_irazabal',
    packages = ['funciones'],
    version = '0.0.1',
    description = 'Paquete con un ejemplo de la estructura que debe tener un paquete para instalarlo con pip',
    install_requires = ['numpy']
)